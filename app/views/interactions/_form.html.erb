<%= simple_form_for [@contact, @interaction] do |f| %>
    <%= f.error_notification %>
    <%= f.input :note %>
    <%= f.input :type_id %>

    <%= f.button :submit, class: "btn-primary" %>
    <%= f.button :button, "Cancel", type: "reset", class: "btn-outline-secondary" %>
<% end %>
